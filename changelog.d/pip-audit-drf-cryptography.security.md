- Patched known CVEs flagged by the `pip-audit` and `osv-scanner` dependency scans. Backend: `djangorestframework` 3.16.1 → 3.17.2 (PYSEC-2026-3827, PYSEC-2026-3828) and `cryptography` 48.0.1 → 50.0.0 (PYSEC-2026-3552, PYSEC-2026-3553, PYSEC-2026-3554), with a new `pyopenssl>=26.0.0,<27` pin — pulled transitively via `daphne`→`twisted`'s TLS support — to keep it compatible with the newer `cryptography`. Frontend: `axios` 1.17.0 → 1.18.0 (GHSA-gcfj-64vw-6mp9, GHSA-f4gw-2p7v-4548, GHSA-hcpx-6fm6-wx23, GHSA-42h9-826w-cgv3, GHSA-7q8q-rj6j-mhjq, GHSA-jqh4-m9w3-8hp9, GHSA-mmx7-hfxf-jppx, GHSA-mwf2-3pr3-8698, GHSA-pmv8-rq9r-6j72, GHSA-xj6q-8x83-jv6g) and `react-router`/`react-router-dom` 7.17.0 → 7.18.3 (GHSA-chx6-hx7r-mcp5, GHSA-h8fp-f39c-q6mh, GHSA-337j-9hxr-rhxg, GHSA-qwww-vcr4-c8h2, GHSA-wrjc-x8rr-h8h6), plus transitive fixes in `linkify-it` (5.0.2), `js-yaml` (4.3.2), `nanoid` (3.3.19), `postcss` (8.5.28), `postcss-selector-parser` (6.1.4), `brace-expansion` (1.1.18 and 5.0.9), `browserslist` (4.28.9), `baseline-browser-mapping` (2.11.23), `@humanfs/node` (0.16.8), and `vitest`/`@vitest/*` (4.1.11, GHSA-82fw-gwwq-j7x9). Also upgrades the rich-text editor from Tiptap 2.27.2 to 3.31.3 (with `tiptap-markdown` 0.8.10 → 0.9.0) to clear GHSA-cp6q-959q-f8rh, a prototype-pollution flaw in `mergeAttributes()` that could turn attacker-supplied attributes into executable DOM attributes; there is no 2.x backport. The upgrade is deliberately behavior-neutral — Tiptap 3's StarterKit bundles Link (with autolinking) and Underline, which Tiptap 2's did not, and both are disabled so that existing card descriptions keep serializing exactly as before. `osv-scanner` and `npm audit` now both report zero findings (#1105).
