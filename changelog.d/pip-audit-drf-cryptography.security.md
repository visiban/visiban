- Patched known CVEs flagged by the scheduled dependency scan. Backend: `djangorestframework` 3.16.1 → 3.17.2 (PYSEC-2026-3827, PYSEC-2026-3828) and `cryptography` 48.0.1 → 50.0.0 (PYSEC-2026-3552, PYSEC-2026-3553, PYSEC-2026-3554), with a new `pyopenssl>=26.0.0` floor — pulled transitively via `daphne`→`twisted`'s TLS support — to keep it compatible with the newer `cryptography`.
