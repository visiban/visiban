Analytics endpoint: five per-card movement aggregates (last-moved-at, last-column, age-mode entry, first-moved-at) are now computed via SQL subqueries so age mode, stall detection, and velocity work correctly without loading all-time movement history.