Custom field create, update, delete, and reorder events are now written to the board change feed, so clients replaying `GET /api/v1/boards/<id>/events/?after=` see schema changes they missed while disconnected.
