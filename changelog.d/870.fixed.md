Fix a `frontend-test` CI flake where the Settings → Behavior "Restart onboarding tour" confirmation timer could fire after the component unmounted, producing an unhandled `ReferenceError: window is not defined` during test teardown. The 4-second timer is now held in a ref and cleared both on unmount and before scheduling a new one. No change to user-visible behavior.
