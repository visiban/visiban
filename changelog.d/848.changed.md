- Board chrome is now organized into two accessible rows: a 56 px app header (logo, breadcrumb, notifications, user) and a 40 px board-chrome row (view tabs, controls, live indicator). The new structure exposes `banner`, `navigation`, `main`, and `search` ARIA landmarks for assistive tech, with a dedicated "Board chrome" navigation label and a "Card filters" search label on the filter row. Foundation-only change — no new controls, no behavior changes. (#848)
