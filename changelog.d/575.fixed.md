Analytics heatmap now shows actual card dwell times on all period views instead of capping every cell at ≥Nd. The period selector controls which transitions are included in the heatmap; dwell is measured from the card's real entry timestamp rather than the window boundary.
