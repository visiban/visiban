Django>=5.2.15,<6
zipp==3.19.1
daphne==4.2.2
# Floor ujson to clear GHSA-c38f-wx89-p2xg / GHSA-c8rr-9gxc-jprv / GHSA-wgvc-ghv9-3pmm /
# GHSA-3j69-69wj-xqx2. Pulled transitively via daphne -> autobahn, which requires only
# ujson>=4.0.2 (unbounded), so without a floor the OSV scanner resolves the vulnerable
# 5.9.0. autobahn imposes no upper bound, so >=5.13.0 is safe.
ujson>=5.13.0
channels==4.3.2
channels-redis==4.3.0
# Pin redis-py below 6: redis-py 8.0.0 enforces a 5s socket read timeout on
# block-forever BZPOPMIN, which channels-redis 4.3.0 relies on, killing idle
# WebSocket connections after 5s. channels-redis only requires redis>=4.6.
redis>=4.6,<6
djangorestframework==3.17.2
psycopg2-binary==2.9.11
django-allauth==65.14.3
dj-rest-auth==7.1.1
django-cors-headers==4.9.0
django-environ==0.13.0
drf-nested-routers==0.94.1
requests==2.33.0
# Pin transitive dep (via requests) to clear GHSA-65pc-fj4g-8rjx; within requests' idna<4 range.
idna==3.18
PyJWT==2.13.0
cryptography==50.0.0
# Floor pyOpenSSL to clear its cap on cryptography (pyOpenSSL 25.3.0 requires
# cryptography<47). Pulled transitively via daphne -> twisted's TLS support,
# which imports OpenSSL.SSL unconditionally at app-load time, so an
# incompatible pair breaks Django startup rather than failing at install time.
# pyOpenSSL is not itself declared elsewhere in this file. Bounded below 27 for
# the same reason: a major bump arriving silently in a rebuild would surface as a
# startup failure, not an install failure.
pyopenssl>=26.0.0,<27
django-filter==25.2
# MCP (Model Context Protocol) server SDK — FastMCP + Streamable HTTP transport
# for the /mcp endpoint (#511). Pinned to the 1.x line: 2.x depends on httpx2,
# a from-scratch httpx successor with no production track record yet. 1.30.0 is
# past all six historical HIGH advisories (last fixed in 1.28.1); do not float
# below that. django-mcp was evaluated and rejected — its upstream repo is
# archived and its own mcp[cli]~=1.9.2 pin forces a vulnerable SDK version.
mcp==1.30.0
# python-multipart arrives transitively via mcp, which declares only
# `python-multipart>=0.0.9` with no upper bound. pip resolves that to the latest
# (0.0.32 today), so built images are not affected — but osv-scanner resolves the
# MINIMUM satisfying version and so reports 0.0.9 with 8 advisories
# (PYSEC-2026-1851/1852 and PYSEC-2026-3036..3041). Nothing guarantees the
# resolver's choice, so floor it explicitly. 0.0.31 is the highest fix version
# across the eight; mcp's unbounded spec accepts it.
python-multipart>=0.0.31
# anyio arrives transitively via mcp (anyio>=4.5, no upper bound) and via
# httpx/starlette/sse-starlette in the same MCP dependency chain, all
# similarly unbounded. pip resolves that to the latest satisfying version
# (4.15.x today), so built images are not affected — but osv-scanner
# resolves the MINIMUM satisfying version and so reports 4.9.0, vulnerable
# to GHSA-82r6-8w77-94w6 (CRITICAL — TLSStream IDNA 2003 hostname encoding
# enables TLS certificate validation bypass) and GHSA-5p39-cfhj-2xmp.
# Floor it explicitly at the first version both advisories are fixed in,
# mirroring the python-multipart pattern above.
anyio>=4.14.2
whitenoise==6.8.2
drf-spectacular==0.29.0
